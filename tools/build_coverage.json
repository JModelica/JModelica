{
  "_comment": "Build systems whose CI jobs must actually run. Generated by tools/check_build_coverage.py --update. This list may only grow: if a build system disappears from the tree its jobs would silently skip and CI would go green having built nothing, so the check fails instead.",
  "required": [
    "cmake",
    "gradle"
  ]
}
